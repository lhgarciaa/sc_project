stages:
- test

test:
  stage: test
  script:
    - whoami
    - pwd
    - "virtualenv testvenv"
    - ". testvenv/bin/activate"
    - "python -m pip install --upgrade pip"
    - "pip install -r requirements.txt"
    - "bash run_tests.sh"
  tags:
    - bla_local
